<div class="max-w-lg mx-auto bg-gray-900 p-8 mt-20 rounded-lg shadow-lg border border-gray-700">
  <h2 class="text-2xl font-bold text-white mb-6 text-center">Recuperar senha 🛠️</h2>

  <p class="text-gray-400 mb-6 text-center">
    Informe seu email e enviaremos um link para redefinir sua senha.
  </p>

  <%= form_for(resource, as: resource_name, url: password_path(resource_name)) do |f| %>
    <div class="mb-5 py-2">
      <%= f.label :email, "Email", class: "text-gray-300 font-medium" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "mt-2 w-full bg-gray-800 text-white border border-gray-700 rounded p-3 focus:ring focus:ring-yellow-500" %>
    </div>

    <%= f.submit "Enviar instruções",
          class: "w-full bg-yellow-500 hover:bg-yellow-600 text-black font-bold p-3 rounded-lg transition cursor-pointer" %>
  <% end %>

  <div class="text-center mt-6 space-y-2">
    <%= link_to "Voltar ao Login", new_session_path(resource_name),
          class: "text-yellow-400 hover:text-yellow-300 font-medium" %>

    <p class="text-gray-400 text-sm">
      Ainda não tem conta?
      <%= link_to "Cadastrar", new_registration_path(resource_name),
            class: "text-yellow-400 hover:text-yellow-300 font-semibold" %>
    </p>
  </div>
</div>
